#include<bits/stdc++.h>
using namespace std;

const int N=1e5+10;
const int MAX=0x3f3f3f3f;
int n,a,b,res,ma=-MAX, mb = MAX;

int main() {
    cin >> n;
    for (int i=0; i<n; i++) {
        cin >> a >> b;
        if(b >= ma && a <= mb)
    }
}