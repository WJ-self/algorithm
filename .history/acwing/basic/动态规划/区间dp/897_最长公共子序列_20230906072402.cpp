#include<bits/stdc++.h>
using namespace std;

const int N = 1010;
int dp[N][N];
int n,m;
int a[N], b[N];

int main() {
    cin >> n >> m;
    for (int i=1; i<=n; i++) {
        cin >> a[i];
    }
    for (int i=1; i<=m; i++) {
        cin >> a[i];
    }
}