#include<bits/stdc++.h>
using namespace std;

const int M = 1010;
int N,V,v[M],w[M],s;

int main() {
    cin >> N >> V;
    for (int i=1; i<=N; i++) {
        cin >> v[i] >> w[i] >> s;
        while (s--){
            
        }
    }
}