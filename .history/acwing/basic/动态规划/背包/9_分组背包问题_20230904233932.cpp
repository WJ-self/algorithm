#include<bits/stdc++.h>
using namespace std;

const int M = 110;
int N,V,S;
int v[M][M], w[M][M], s[];

int main() {
    cin >> N >> V;
    for (int i=1; i<=N; i++) {
        cin >> S;
        for (int j=1; j<=S; j++){
            cin >> v[i][j] >> w[i][j];
        }
    }

    for (int i=1; i<N; i++) {
        for (int j=1; j<=M; j++) {
            for (int k=)
        }
    }
}
