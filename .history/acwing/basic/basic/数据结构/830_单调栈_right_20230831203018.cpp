#include<bits/stdc++.h>
using namespace std;

stack<int> st;
const int N = 1e5;
int a[N];

int main(){
    int M,x;
    cin >> M;
    for (int i=0; i<M; i++){
        cin >> x;
        while(!st.empty() && st.top() >= x) {

        }
        if(st.empty()) {
            a[i] = 
        }
        else {

        }
    }
}