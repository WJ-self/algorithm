#include<bits/stdc++.h>
using namespace std;

const int N = 1e5+10;
int qu[N];
int i,j;

int main() {
    int M;
    cin >> M;
    string op;
    int x;
    while(M--){
        cin >> op;
        if(op == "push") {
            cin >> x;
            qu[j++] = 
        }
    }
}
