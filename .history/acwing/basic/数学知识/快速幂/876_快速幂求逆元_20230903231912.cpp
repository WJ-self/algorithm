#include<bits/stdc++.h>
using namespace std;

int n;
typedef long long LL;
LL a,p;

void quick_pow(LL a, LL p){
    
}

int main() {
    cin >> n;
    while(n--) {
        cin >> a >> p;
        quick_pow(a,p);
    }
    return 0;
}