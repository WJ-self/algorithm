#include<bits/stdc++.h>
using namespace std;

void divide(int x){
    if(x < 2) return;
    for(int i=2; i<x; i++) {
        if(x % i == 0) {
            
        }
    }
}

int main(){
    int n,x;
    cin >> n;
    while(n--){
        cin >> x;
        divide(x);
    }
    return 0;
}