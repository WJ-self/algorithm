#include<bits/stdc++.h>
using namespace std;

int n;

bool isPrime(int x) {
    if(x <)
}

int main() {
    cin >> n;
    int x;
    for (int i=0; i<n; i++) {
        cin >> x;
        isPrime(x);
    }
    return 0;
}